apiVersion: v1
data:
  $patch: replace # We do not need some of the original fields.
  admin: manpham130699@gmail.com # kpt-set: ${email}
  gcp-sa: kubeflow-user@coach-on-tap.iam.gserviceaccount.com # kpt-set: ${name}-user@${gcloud.core.project}.iam.gserviceaccount.com
kind: ConfigMap
metadata:
  name: config
